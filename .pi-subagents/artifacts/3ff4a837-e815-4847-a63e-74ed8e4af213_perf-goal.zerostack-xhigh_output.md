**Finding: high severity -- stale evidence is mitigated, not superseded.** `effef17` limits children per parent; independent parents can still each hold a full same-repo index.

- Entry point: `crates/tokenzero/src/bin/tokenzero_codemode.rs::main`
- No machine-permit/RSS regression exists under `crates/tokenzero/tests/`.
- Highest Amdahl impact: cap resident indexes at two per canonical repository. Prior measurements imply 75% replica-RSS removal at 8 clients, 87.5% at 16, 95.5% at 44.

**Minimal safe patch**

1. Add `MachineIndexPermit` and `acquire_machine_index_permit()` beside CodeMode index initialization.
2. Hash canonical repository root into a per-user runtime directory.
3. Acquire either of two advisory lock files before constructing the index; hold the `File` guard for index lifetime.
4. Queue excess initialization. Advisory locks automatically release on crash.
5. Add focused multi-process test `crates/tokenzero/tests/codemode_machine_permits.rs`.

Risk: startup/request queueing and incorrect repository-key canonicalization, especially Git worktrees. Shared mmap is higher-risk and unnecessary for the first bounded-RSS fix.

Acceptance command:

```bash
cargo test -p tokenzero --test codemode_machine_permits -- --nocapture
```