# ZeroStack derivation provenance v1 (frozen)

Freeze id: `zerostack.derivation-provenance.v1`  
Bead: `tokenzero-cas-gc-vnext-provenance-2yis`

Orthogonal to `zerostack.cas-gc.v1` (`../shared-cas-gc/v1/`). Do not place these records under `gc/roots|pins|leases`.

Contents: `SPEC.md`, `derivation-provenance.schema.json`, `fixtures/`.

Validate:

```bash
python3 schemas/derivation-provenance/v1/fixtures/validate_fixtures.py
```
