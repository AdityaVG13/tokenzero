surface-codemode feature test lane is red at HEAD (15 stable failures + flaky set)
open

Baseline run on HEAD (commit 734be9b, Spark, 2026-07-28): cargo test -p tokenzero-mcp --no-default-features --features surface-codemode --lib codemode => 62 passed / 17 failed. 15 failures reproduce on both HEAD and the osn1 patch (bench_harness x9 incl. fastmcp-v2 byte-parity comparisons, e2e envelope_v3, sentinel production_takeover, exec accumulator_bounds, quickjs_mutation_classifier x4); 2 more flip between runs (promise_all_runs_independent_shells_concurrently, shell_ref_is_canonical passed 6/6 in isolated reruns; accumulator_bounds + microtask_cap_error flaked baseline-side). The workspace default feature is surface-mcp, so `cargo test --workspace` never exercises this lane; the mutual-exclusion compile_error (tokenzero-irx9.3) means codemode tests only run with --no-default-features --features surface-codemode. This lane must be green before tokenzero-codemode ships as the default install surface (4uql.7) and before the crate extraction (4uql.8.1). Acceptance: the suite passes on a quiet worker; the 2 flaky tests get deterministic synchronization or a documented quarantine with tracking.